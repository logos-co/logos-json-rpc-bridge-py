<!-- GENERATED by logos-bridge-codegen from the LIDL contract test_fullapi_ext_cpp 1.0.0. Do not edit. -->
<!-- Source: lidl tests/fixtures/lidl/test_fullapi_ext_cpp.lidl; generator: logos-bridge 0.1.0; reader: lidl 0.1.0 (2043d8b) -->

# `test_fullapi_ext_cpp`

C++ mirror of test_fullapi_ext_rust, HEADER-FIRST like test_fullapi_cpp: the contract is derived from the impl header, records included. Keeps the two ext providers comparable cell for cell.

| | |
|---|---|
| Version | `1.0.0` |
| Category | `testing` |
| Depends on | nothing |
| Python clients | `AsyncTestFullapiExtCppClient`, `TestFullapiExtCppClient` |
| `interface_sha256` | `435ba4bdc7c854b718a2584e4252cbe6a25b85727286b890b6dfccb29f238e5d` |
| `contract_sha256` | `7308b6432d35a7e367dc33219e9c059c59faac89a3aabd05b2b6984f934bd1fe` |
| `shape_sha256` | `7ef826b134ad1ef775d8b396d8ff669671fea5255383e9b0725fe6ad966ede16` |

Calls go through `rpc.call`; `client` is an `AsyncTestFullapiExtCppClient` (await the calls)
or a `TestFullapiExtCppClient` (the same calls, blocking).

## Methods

| Method | Python | Returns |
|---|---|---|
| [`whoAmI`](#method-whoAmI) | `await client.who_am_i()` | `str` |
| [`echoBlob`](#method-echoBlob) | `await client.echo_blob(v)` | `Blob` |
| [`echoWrapper`](#method-echoWrapper) | `await client.echo_wrapper(v)` | `Wrapper` |
| [`echoBlobList`](#method-echoBlobList) | `await client.echo_blob_list(v)` | `list[Blob]` |
| [`echoBlobMap`](#method-echoBlobMap) | `await client.echo_blob_map(v)` | `dict[str, Blob]` |
| [`echoBytesList`](#method-echoBytesList) | `await client.echo_bytes_list(v)` | `list[bytes]` |
| [`echoBytesMap`](#method-echoBytesMap) | `await client.echo_bytes_map(v)` | `dict[str, bytes]` |
| [`echoIntMap`](#method-echoIntMap) | `await client.echo_int_map(v)` | `dict[str, int]` |
| [`echoStringMap`](#method-echoStringMap) | `await client.echo_string_map(v)` | `dict[str, str]` |
| [`echoNestedInts`](#method-echoNestedInts) | `await client.echo_nested_ints(v)` | `list[list[int]]` |
| [`echoMapOfBytesLists`](#method-echoMapOfBytesLists) | `await client.echo_map_of_bytes_lists(v)` | `dict[str, list[bytes]]` |
| [`echoOpt`](#method-echoOpt) | `await client.echo_opt(v)` | `Opt` |
| [`echoOptList`](#method-echoOptList) | `await client.echo_opt_list(v)` | `list[Opt]` |
| [`echoOptional`](#method-echoOptional) | `await client.echo_optional(v)` | `str \| None` |
| [`fireBlobEvent`](#method-fireBlobEvent) | `await client.fire_blob_event(v)` | `bool` |
| [`name`](#method-name) | `await client.name()` | `str` |
| [`version`](#method-version) | `await client.version()` | `str` |
| [`lidl`](#method-lidl) | `await client.lidl()` | `str` |

<a id="method-whoAmI"></a>

### `whoAmI`

```lidl
method whoAmI() -> tstr
```

**Returns** `tstr` (Python `str`).

Python: `await client.who_am_i()`

<a id="method-echoBlob"></a>

### `echoBlob`

```lidl
method echoBlob(v: Blob) -> Blob
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `Blob` | `v: Blob` |

**Returns** `Blob` (Python `Blob`).

Python: `await client.echo_blob(v)`

<a id="method-echoWrapper"></a>

### `echoWrapper`

```lidl
method echoWrapper(v: Wrapper) -> Wrapper
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `Wrapper` | `v: Wrapper` |

**Returns** `Wrapper` (Python `Wrapper`).

Python: `await client.echo_wrapper(v)`

<a id="method-echoBlobList"></a>

### `echoBlobList`

```lidl
method echoBlobList(v: [Blob]) -> [Blob]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[Blob]` | `v: Sequence[Blob]` |

**Returns** `[Blob]` (Python `list[Blob]`).

Python: `await client.echo_blob_list(v)`

<a id="method-echoBlobMap"></a>

### `echoBlobMap`

```lidl
method echoBlobMap(v: {tstr: Blob}) -> {tstr: Blob}
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `{tstr: Blob}` | `v: Mapping[str, Blob]` |

**Returns** `{tstr: Blob}` (Python `dict[str, Blob]`).

Python: `await client.echo_blob_map(v)`

<a id="method-echoBytesList"></a>

### `echoBytesList`

```lidl
method echoBytesList(v: [bstr]) -> [bstr]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[bstr]` | `v: Sequence[bytes]` |

**Returns** `[bstr]` (Python `list[bytes]`).

Python: `await client.echo_bytes_list(v)`

<a id="method-echoBytesMap"></a>

### `echoBytesMap`

```lidl
method echoBytesMap(v: {tstr: bstr}) -> {tstr: bstr}
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `{tstr: bstr}` | `v: Mapping[str, bytes]` |

**Returns** `{tstr: bstr}` (Python `dict[str, bytes]`).

Python: `await client.echo_bytes_map(v)`

<a id="method-echoIntMap"></a>

### `echoIntMap`

```lidl
method echoIntMap(v: {tstr: int}) -> {tstr: int}
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `{tstr: int}` | `v: Mapping[str, int]` |

**Returns** `{tstr: int}` (Python `dict[str, int]`).

Python: `await client.echo_int_map(v)`

<a id="method-echoStringMap"></a>

### `echoStringMap`

```lidl
method echoStringMap(v: {tstr: tstr}) -> {tstr: tstr}
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `{tstr: tstr}` | `v: Mapping[str, str]` |

**Returns** `{tstr: tstr}` (Python `dict[str, str]`).
A legitimate value shaped like a provider refusal (`{code, message, origin}` with a known code) is reported as `ProviderRejection`.

Python: `await client.echo_string_map(v)`

<a id="method-echoNestedInts"></a>

### `echoNestedInts`

```lidl
method echoNestedInts(v: [[int]]) -> [[int]]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[[int]]` | `v: Sequence[Sequence[int]]` |

**Returns** `[[int]]` (Python `list[list[int]]`).

Python: `await client.echo_nested_ints(v)`

<a id="method-echoMapOfBytesLists"></a>

### `echoMapOfBytesLists`

```lidl
method echoMapOfBytesLists(v: {tstr: [bstr]}) -> {tstr: [bstr]}
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `{tstr: [bstr]}` | `v: Mapping[str, Sequence[bytes]]` |

**Returns** `{tstr: [bstr]}` (Python `dict[str, list[bytes]]`).

Python: `await client.echo_map_of_bytes_lists(v)`

<a id="method-echoOpt"></a>

### `echoOpt`

```lidl
method echoOpt(v: Opt) -> Opt
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `Opt` | `v: Opt` |

**Returns** `Opt` (Python `Opt`).

Python: `await client.echo_opt(v)`

<a id="method-echoOptList"></a>

### `echoOptList`

```lidl
method echoOptList(v: [Opt]) -> [Opt]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[Opt]` | `v: Sequence[Opt]` |

**Returns** `[Opt]` (Python `list[Opt]`).

Python: `await client.echo_opt_list(v)`

<a id="method-echoOptional"></a>

### `echoOptional`

```lidl
method echoOptional(v: ? tstr) -> ? tstr
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `? tstr` (optional) | `v: str \| None` |

**Returns** `? tstr` (Python `str \| None`).

Python: `await client.echo_optional(v)`

<a id="method-fireBlobEvent"></a>

### `fireBlobEvent`

```lidl
method fireBlobEvent(v: Blob) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `Blob` | `v: Blob` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_blob_event(v)`

<a id="method-name"></a>

### `name`

```lidl
method name() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's name, as declared in its metadata.

**Returns** `tstr` (Python `str`).

Python: `await client.name()`

<a id="method-version"></a>

### `version`

```lidl
method version() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's version, as declared in its metadata.

**Returns** `tstr` (Python `str`).

Python: `await client.version()`

<a id="method-lidl"></a>

### `lidl`

```lidl
method lidl() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's canonical LIDL interface document.

**Returns** `tstr` (Python `str`).

Python: `await client.lidl()`

## Events

<a id="event-blobEvent"></a>

### `blobEvent`

```lidl
event blobEvent(v: Blob)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `Blob` | `v: Blob` |

Python: `async with client.on_blob_event() as events:` yields `BlobEventEvent`; `client.events('blobEvent')` merges it with others.

## Records

<a id="record-Blob"></a>

### `Blob`

Python: the frozen dataclass `Blob`. An empty optional field is left out of the JSON object.

| Field | LIDL | Python attribute | Optional |
|---|---|---|---|
| `id` | `id: tstr` | `id: str` | no |
| `n` | `n: uint` | `n: int` | no |
| `payload` | `payload: bstr` | `payload: bytes` | no |

<a id="record-Wrapper"></a>

### `Wrapper`

Python: the frozen dataclass `Wrapper`. An empty optional field is left out of the JSON object.

| Field | LIDL | Python attribute | Optional |
|---|---|---|---|
| `inner` | `inner: Blob` | `inner: Blob` | no |
| `tags` | `tags: [tstr]` | `tags: list[str]` | no |
| `blobs` | `blobs: [Blob]` | `blobs: list[Blob]` | no |

<a id="record-Opt"></a>

### `Opt`

Python: the frozen dataclass `Opt`. An empty optional field is left out of the JSON object.

| Field | LIDL | Python attribute | Optional |
|---|---|---|---|
| `required` | `required: tstr` | `required: str` | no |
| `maybe` | `maybe: ? tstr` | `maybe: str \| None` | yes |
| `count` | `count: ? uint` | `count: int \| None` | yes |
| `blob` | `blob: ? bstr` | `blob: bytes \| None` | yes |
