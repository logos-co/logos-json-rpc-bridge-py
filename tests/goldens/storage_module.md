<!-- GENERATED by logos-bridge-codegen from the LIDL contract storage_module 2.1.3. Do not edit. -->
<!-- Source: lidl tests/fixtures/lidl/storage_module.lidl; generator: logos-bridge 0.1.0; reader: lidl 0.1.0 (2043d8b) -->

# `storage_module`

Storage module

| | |
|---|---|
| Version | `2.1.3` |
| Category | `protocol` |
| Depends on | nothing |
| Python clients | `AsyncStorageModuleClient`, `StorageModuleClient` |
| `interface_sha256` | `f3e9b89b4fe0d9f988bdd24bad92c56a51178660ef0e4f68ef54b1261a452037` |
| `contract_sha256` | `9f6bd141a1401b14ec151b579fd1e1076ba7916929f54101fd6843501dee92ac` |
| `shape_sha256` | `2857f750e8a125b4742c008a264cc212f7ecb5bf5b86905a1bfa0532bc69a42c` |

Calls go through `rpc.call`; `client` is an `AsyncStorageModuleClient` (await the calls)
or a `StorageModuleClient` (the same calls, blocking).

## Methods

| Method | Python | Returns |
|---|---|---|
| [`migrateConfig`](#method-migrateConfig) | `await client.migrate_config(cfg)` | `LogosResult[Any]` |
| [`init`](#method-init) | `await client.init(cfg)` | `bool` |
| [`start`](#method-start) | `await client.start()` | `bool` |
| [`stop`](#method-stop) | `await client.stop()` | `LogosResult[Any]` |
| [`destroy`](#method-destroy) | `await client.destroy()` | `LogosResult[Any]` |
| [`libstorageVersion`](#method-libstorageVersion) | `await client.libstorage_version()` | `LogosResult[Any]` |
| [`moduleVersion`](#method-moduleVersion) | `await client.module_version()` | `str` |
| [`dataDir`](#method-dataDir) | `await client.data_dir()` | `LogosResult[Any]` |
| [`network`](#method-network) | `await client.network()` | `LogosResult[Any]` |
| [`peerId`](#method-peerId) | `await client.peer_id()` | `LogosResult[Any]` |
| [`spr`](#method-spr) | `await client.spr()` | `LogosResult[Any]` |
| [`debug`](#method-debug) | `await client.debug()` | `LogosResult[Any]` |
| [`collectMetrics`](#method-collectMetrics) | `await client.collect_metrics()` | `dict[str, Any]` |
| [`updateLogLevel`](#method-updateLogLevel) | `await client.update_log_level(log_level)` | `LogosResult[Any]` |
| [`connect`](#method-connect) | `await client.connect(peer_id, peer_addresses)` | `LogosResult[Any]` |
| [`togglePrivateQueries`](#method-togglePrivateQueries) | `await client.toggle_private_queries(enabled)` | `LogosResult[Any]` |
| [`uploadUrl`](#method-uploadUrl) | `await client.upload_url(file_path, chunk_size)` | `LogosResult[Any]` |
| [`uploadInit`](#method-uploadInit) | `await client.upload_init(filename, chunk_size)` | `LogosResult[Any]` |
| [`uploadChunk`](#method-uploadChunk) | `await client.upload_chunk(session_id, chunk)` | `LogosResult[Any]` |
| [`uploadFinalize`](#method-uploadFinalize) | `await client.upload_finalize(session_id)` | `LogosResult[Any]` |
| [`uploadCancel`](#method-uploadCancel) | `await client.upload_cancel(session_id)` | `LogosResult[Any]` |
| [`downloadToUrl`](#method-downloadToUrl) | `await client.download_to_url(cid, file_path, local, chunk_size)` | `LogosResult[Any]` |
| [`downloadChunks`](#method-downloadChunks) | `await client.download_chunks(cid, local, chunk_size)` | `LogosResult[Any]` |
| [`downloadCancel`](#method-downloadCancel) | `await client.download_cancel(session_id)` | `LogosResult[Any]` |
| [`exists`](#method-exists) | `await client.exists(cid)` | `LogosResult[Any]` |
| [`fetch`](#method-fetch) | `await client.fetch(cid)` | `LogosResult[Any]` |
| [`remove`](#method-remove) | `await client.remove(cid)` | `LogosResult[Any]` |
| [`space`](#method-space) | `await client.space()` | `LogosResult[Any]` |
| [`manifests`](#method-manifests) | `await client.manifests()` | `LogosResult[Any]` |
| [`downloadManifest`](#method-downloadManifest) | `await client.download_manifest(cid)` | `LogosResult[Any]` |
| [`importFiles`](#method-importFiles) | `await client.import_files(path)` | nothing |
| [`name`](#method-name) | `await client.name()` | `str` |
| [`version`](#method-version) | `await client.version()` | `str` |
| [`lidl`](#method-lidl) | `await client.lidl()` | `str` |

<a id="method-migrateConfig"></a>

### `migrateConfig`

```lidl
method migrateConfig(cfg: tstr) -> result
```

Migrate the configuration after a Storage Module update.

This method takes a configuration string in parameters and returns
a new configuration JSON string updated.

Depending on the updates of logos-storage-nim, some options can be removed
or replaced with new ones. This method ensures a migration path for the
configuration to the latest version of the module using a configuration version.

In addition to the migration, the data-dir is also set if it is not provided
in configuration. And with `mix-enabled` true plus a network key, the mix
configuration is updated.

This method should be called before `init()`. It is a separate step so
the caller can get the result of the new configuration and update it.

Returns StdLogosResult::value as the JSON to hand to init().

| Parameter | LIDL | Python |
|---|---|---|
| `cfg` | `tstr` | `cfg: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.migrate_config(cfg)`

<a id="method-init"></a>

### `init`

```lidl
method init(cfg: tstr) -> bool
```

Create a new storage node instance and configure it.

`cfg` is a JSON string with the configuration overwriting defaults.

Example of JSON config:

```json
{
"log-level": "info",
"log-format": "auto",
"metrics": false,
"metrics-address": "127.0.0.1",
"metrics-port": 8008,
"data-dir": ".cache/storage",
"listen-ip": "0.0.0.0",
"listen-port": 0,
"nat": "auto",
"net-privkey": "key",
"bootstrap-node": [],
"no-bootstrap-node": false,
"network": "logos.test",
"dht-mix-proxy": [],
"mix-enabled": false,
"mix-pool": "",
"mix-pool-json": "",
"max-peers": 160,
"num-threads": 0,
"agent-string": "Logos Storage",
"repo-kind": "fs",
"storage-quota": 21474836480,
"block-ttl": "30d",
"block-mi": "10m",
"block-mn": 1000,
"block-retries": 300,
"log-file": "/tmp/storage-log-624036264.log",
"nat-schedule-interval": "2m",
"nat-num-peers-to-ask": 3,
"nat-max-queue-size": 3,
"nat-min-confidence": 0.6,
"nat-observed-addr-min-count": 1,
"nat-max-relays": 2,
"nat-port-mapping-discover-timeout": 500,
"nat-port-mapping-timeout": 500,
"nat-port-mapping-recheck-period": 300000
}
```

Do not call init() more than once per instance.

Returns true on success.  The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `cfg` | `tstr` | `cfg: str` |

**Returns** `bool` (Python `bool`).

Python: `await client.init(cfg)`

<a id="method-start"></a>

### `start`

```lidl
method start() -> bool
```

Start the storage node.

Returns true if the start command was accepted by libstorage.  Actual
completion is signalled asynchronously via the `storageStart` event.

The method is asynchronous.

**Returns** `bool` (Python `bool`).

Python: `await client.start()`

<a id="method-stop"></a>

### `stop`

```lidl
method stop() -> result
```

Stop the storage node.

The node can be started and stopped multiple times.  Returns a
StdLogosResult indicating whether the stop command was sent; actual
completion is signalled via the `storageStop` event.

The method is asynchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.stop()`

<a id="method-destroy"></a>

### `destroy`

```lidl
method destroy() -> result
```

Destroy the storage context and free all resources.

Internally calls storage_close then storage_destroy.  The node should
be stopped before calling destroy().  Not stopping first can lead to
undefined behaviour (e.g. data loss or crashes).

Returns StdLogosResult::success = true on success.
The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.destroy()`

<a id="method-libstorageVersion"></a>

### `libstorageVersion`

```lidl
method libstorageVersion() -> result
```

Get the libstorage version string.

Named for what it returns: this is the version of the underlying
libstorage (Nim) library, NOT of this module. `version()` cannot be used
for it -- that name is reserved for module identity and must be
`version() -> tstr`, which the generator injects from `metadata.json`.

Does not require the node to be started.

Returns StdLogosResult::value as a std::string on success.
The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.libstorage_version()`

<a id="method-moduleVersion"></a>

### `moduleVersion`

```lidl
method moduleVersion() -> tstr
```

Get this module's version, as declared in `metadata.json`.

The method is synchronous.

**Returns** `tstr` (Python `str`).

Python: `await client.module_version()`

<a id="method-dataDir"></a>

### `dataDir`

```lidl
method dataDir() -> result
```

Get the storage data directory path.

Returns StdLogosResult::value as a std::string on success.
The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.data_dir()`

<a id="method-network"></a>

### `network`

```lidl
method network() -> result
```

Get the Logos network this node was configured for.

Returns StdLogosResult::value as a std::string on success.
The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.network()`

<a id="method-peerId"></a>

### `peerId`

```lidl
method peerId() -> result
```

Get the node's peer ID.

The peer ID is the libp2p peer identity as described at
https://docs.libp2p.io/concepts/fundamentals/peers/

Returns StdLogosResult::value as a std::string on success.
The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.peer_id()`

<a id="method-spr"></a>

### `spr`

```lidl
method spr() -> result
```

Get the node's Signed Peer Record (SPR).

Returns StdLogosResult::value as a std::string on success.
The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.spr()`

<a id="method-debug"></a>

### `debug`

```lidl
method debug() -> result
```

Get debug information for the node.

Returns StdLogosResult::value as a JSON object on success:

```json
{
"id": string,
"addrs": [string],
"spr": string,
"table": {
"localNode": { "peerId": string, "addresses": [string],
"lastSeen": null },
"nodes": [{ "peerId": string, "addresses": [string],
"lastSeen": int }]
}
}
```

The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.debug()`

<a id="method-collectMetrics"></a>

### `collectMetrics`

```lidl
method collectMetrics() -> {tstr: any}
```

Collect node metrics for the openmetrics module.

Implements the openmetrics-module IMetricsSource interface. Returns a
Logos openmetrics-compatible JSON object. On libstorage errors or invalid
payloads, returns: { "metrics": [] }.

```json
{
"metrics": [
{
"name": string,
"type": string,
"help": string,
"value": number,
"labels": object
}
]
}
```

The method is synchronous.

**Returns** `{tstr: any}` (Python `dict[str, Any]`).
A legitimate value shaped like a provider refusal (`{code, message, origin}` with a known code) is reported as `ProviderRejection`.

Python: `await client.collect_metrics()`

<a id="method-updateLogLevel"></a>

### `updateLogLevel`

```lidl
method updateLogLevel(logLevel: tstr) -> result
```

Set the log level at runtime.

`logLevel` must be one of: TRACE, DEBUG, INFO, NOTICE, WARN, ERROR, FATAL

Returns StdLogosResult::success = true on success.
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `logLevel` | `tstr` | `log_level: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.update_log_level(log_level)`

<a id="method-connect"></a>

### `connect`

```lidl
method connect(peerId: tstr, peerAddresses: [tstr]) -> result
```

Connect to a peer.

Uses `peerAddresses` as explicit dial targets when provided; otherwise
the peer must be discoverable via the DHT using `peerId`.

Returns a StdLogosResult indicating whether the connect command was sent;
actual completion is signalled via the `storageConnect` event.

The method is asynchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `peerId` | `tstr` | `peer_id: str` |
| `peerAddresses` | `[tstr]` | `peer_addresses: Sequence[str]` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.connect(peer_id, peer_addresses)`

<a id="method-togglePrivateQueries"></a>

### `togglePrivateQueries`

```lidl
method togglePrivateQueries(enabled: bool) -> result
```

Toggle routing of DHT queries over the Logos mix network.

When enabled, all subsequent DHT queries are tunnelled over Mix; this
affects queries only, not advertisements.

Enabling requires Mix to be configured: `mix-enabled` true and at
least one `dht-mix-proxy` set (see init()). Otherwise enabling fails
with an error. Disabling is always allowed.

This is a temporary API and will likely be removed before mainnet.

On success, returns StdLogosResult::value as a bool: the previous toggle
state (true = private queries were already enabled).
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `enabled` | `bool` | `enabled: bool` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.toggle_private_queries(enabled)`

<a id="method-uploadUrl"></a>

### `uploadUrl`

```lidl
method uploadUrl(filePath: tstr, chunkSize: int) -> result
```

Upload a local file by absolute path.

Internally calls storage_upload_init followed by storage_upload_file.
If init succeeds but the file upload command fails, the session is
cancelled automatically.

`filePath`  – absolute path to the file on disk.
`chunkSize` – upload chunk size in bytes (default 65536).

Returns StdLogosResult::value as a session ID string on success.

The method is asynchronous; progress is signalled via the
`storageUploadProgress` event (throttled to at most one event per %
point) and completion via the `storageUploadDone` event.

| Parameter | LIDL | Python |
|---|---|---|
| `filePath` | `tstr` | `file_path: str` |
| `chunkSize` | `int` | `chunk_size: int` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.upload_url(file_path, chunk_size)`

<a id="method-uploadInit"></a>

### `uploadInit`

```lidl
method uploadInit(filename: tstr, chunkSize: int) -> result
```

Create a manual upload session for chunk-by-chunk streaming.

Use this only when uploadUrl() cannot be used (e.g. you are streaming
data that is not on disk).  After creating a session, send all chunks
with uploadChunk(), then call uploadFinalize() to get the CID.

`filename`  – used to populate manifest metadata (mimetype, name).
`chunkSize` – upload chunk size in bytes (default 65536).

Returns StdLogosResult::value as the session ID string on success.
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `filename` | `tstr` | `filename: str` |
| `chunkSize` | `int` | `chunk_size: int` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.upload_init(filename, chunk_size)`

<a id="method-uploadChunk"></a>

### `uploadChunk`

```lidl
method uploadChunk(sessionId: tstr, chunk: tstr) -> result
```

Upload a single data chunk for a session created with uploadInit().

A failed chunk does not corrupt the session; the caller may retry or
call uploadCancel().

Emits the `storageUploadProgress` event on completion.

The method is asynchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `sessionId` | `tstr` | `session_id: str` |
| `chunk` | `tstr` | `chunk: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.upload_chunk(session_id, chunk)`

<a id="method-uploadFinalize"></a>

### `uploadFinalize`

```lidl
method uploadFinalize(sessionId: tstr) -> result
```

Finalize a manual upload session and retrieve the CID.

Must be called after all chunks have been sent with uploadChunk().

Returns StdLogosResult::value as the CID string on success.
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `sessionId` | `tstr` | `session_id: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.upload_finalize(session_id)`

<a id="method-uploadCancel"></a>

### `uploadCancel`

```lidl
method uploadCancel(sessionId: tstr) -> result
```

Cancel an ongoing upload session.

Returns StdLogosResult::success = true on success.
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `sessionId` | `tstr` | `session_id: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.upload_cancel(session_id)`

<a id="method-downloadToUrl"></a>

### `downloadToUrl`

```lidl
method downloadToUrl(cid: tstr, filePath: tstr, local: bool, chunkSize: int) -> result
```

Download content by CID and write it to a local file.

Internally fetches the manifest first to obtain the total size (required
for progress throttling); returns an error if the manifest is unavailable.

`cid`       – content identifier to download.
`filePath`  – destination path on disk.
`local`     – if true, only reads from locally cached data (no network).
`chunkSize` – download chunk size in bytes (default 65536).

Returns StdLogosResult::value as the session ID (= CID) on success.

The method is asynchronous; progress is signalled via the
`storageDownloadProgress` event (throttled to at most one event per %
point) and completion via the `storageDownloadDone` event.

| Parameter | LIDL | Python |
|---|---|---|
| `cid` | `tstr` | `cid: str` |
| `filePath` | `tstr` | `file_path: str` |
| `local` | `bool` | `local: bool` |
| `chunkSize` | `int` | `chunk_size: int` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.download_to_url(cid, file_path, local, chunk_size)`

<a id="method-downloadChunks"></a>

### `downloadChunks`

```lidl
method downloadChunks(cid: tstr, local: bool, chunkSize: int) -> result
```

Download content by CID and deliver it as a stream of base64-encoded chunks.

Use this when you want to process or forward the data without writing it
to disk.  For large files, downloadToUrl() is more efficient as it avoids
the base64 encoding overhead.

`cid`       – content identifier to download.
`local`     – if true, only reads from locally cached data (no network).
`chunkSize` – download chunk size in bytes (default 65536).

Returns StdLogosResult::value as the session ID (= CID) on success.

The method is asynchronous; each chunk is delivered via the
`storageDownloadProgress` event (one event per chunk, not throttled) and
completion via the `storageDownloadDone` event.

| Parameter | LIDL | Python |
|---|---|---|
| `cid` | `tstr` | `cid: str` |
| `local` | `bool` | `local: bool` |
| `chunkSize` | `int` | `chunk_size: int` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.download_chunks(cid, local, chunk_size)`

<a id="method-downloadCancel"></a>

### `downloadCancel`

```lidl
method downloadCancel(sessionId: tstr) -> result
```

Cancel an ongoing download session.

Returns StdLogosResult::success = true on success.
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `sessionId` | `tstr` | `session_id: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.download_cancel(session_id)`

<a id="method-exists"></a>

### `exists`

```lidl
method exists(cid: tstr) -> result
```

Check whether content identified by CID exists in local storage.

Returns StdLogosResult::value as bool (true = exists) on success.
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `cid` | `tstr` | `cid: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.exists(cid)`

<a id="method-fetch"></a>

### `fetch`

```lidl
method fetch(cid: tstr) -> result
```

Fetch content from the network and cache it locally in the background.

The method returns as soon as the fetch request is accepted; no event is
emitted when the background download completes.

Returns StdLogosResult::success = true if the request was accepted.
The method is synchronous.

| Parameter | LIDL | Python |
|---|---|---|
| `cid` | `tstr` | `cid: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.fetch(cid)`

<a id="method-remove"></a>

### `remove`

```lidl
method remove(cid: tstr) -> result
```

Remove content identified by CID from local storage in the background.

The delete may touch the network and can take a while, so this method
does not block: the returned StdLogosResult only reports whether the
command was dispatched. The real outcome arrives later via the
`storageRemoveDone` event.

| Parameter | LIDL | Python |
|---|---|---|
| `cid` | `tstr` | `cid: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.remove(cid)`

<a id="method-space"></a>

### `space`

```lidl
method space() -> result
```

Get storage space information.

Returns StdLogosResult::value as a JSON object on success:

```json
{
"totalBlocks":        number,
"quotaMaxBytes":      number,
"quotaUsedBytes":     number,
"quotaReservedBytes": number
}
```

The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.space()`

<a id="method-manifests"></a>

### `manifests`

```lidl
method manifests() -> result
```

List all manifests stored locally.

Returns StdLogosResult::value as a JSON array on success; each item:

```json
{
"cid":         string,
"treeCid":     string,
"datasetSize": number,
"blockSize":   number,
"filename":    string,
"mimetype":    string
}
```

The method is synchronous.

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.manifests()`

<a id="method-downloadManifest"></a>

### `downloadManifest`

```lidl
method downloadManifest(cid: tstr) -> result
```

Fetch the manifest for a given CID in the background.

The lookup may query the DHT and can take a long time, so this method
does not block: the returned StdLogosResult only reports whether the
command was dispatched. The real outcome arrives later via the
`storageDownloadManifestDone` event.

| Parameter | LIDL | Python |
|---|---|---|
| `cid` | `tstr` | `cid: str` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.download_manifest(cid)`

<a id="method-importFiles"></a>

### `importFiles`

```lidl
method importFiles(path: tstr)
```

Import all files from a directory (headless helper).

Iterates regular files in `path` and calls uploadUrl() for each.
Does not wait for uploads to complete; listen for `storageUploadDone`
events to track results.

| Parameter | LIDL | Python |
|---|---|---|
| `path` | `tstr` | `path: str` |

**Returns** nothing (the provider answers `true`; Python gets `None`).

Python: `await client.import_files(path)`

<a id="method-name"></a>

### `name`

```lidl
method name() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's name, as declared in its metadata.

**Returns** `tstr` (Python `str`).

Python: `await client.name()`

<a id="method-version"></a>

### `version`

```lidl
method version() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's version, as declared in its metadata.

**Returns** `tstr` (Python `str`).

Python: `await client.version()`

<a id="method-lidl"></a>

### `lidl`

```lidl
method lidl() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's canonical LIDL interface document.

**Returns** `tstr` (Python `str`).

Python: `await client.lidl()`

## Events

<a id="event-storageStart"></a>

### `storageStart`

```lidl
event storageStart(payload: tstr)
```

Emitted when start() has finished starting the node.

```json
{
"success": bool,
"message": string
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_start() as events:` yields `StorageStartEvent`; `client.events('storageStart')` merges it with others.

<a id="event-storageStop"></a>

### `storageStop`

```lidl
event storageStop(payload: tstr)
```

Emitted when stop() has finished stopping the node.

```json
{
"success": bool,
"message": string
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_stop() as events:` yields `StorageStopEvent`; `client.events('storageStop')` merges it with others.

<a id="event-storageConnect"></a>

### `storageConnect`

```lidl
event storageConnect(payload: tstr)
```

Emitted when connect() has finished connecting to the peer.

```json
{
"success": bool,
"message": string
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_connect() as events:` yields `StorageConnectEvent`; `client.events('storageConnect')` merges it with others.

<a id="event-storageUploadProgress"></a>

### `storageUploadProgress`

```lidl
event storageUploadProgress(payload: tstr)
```

Emitted as uploadUrl() or uploadChunk() upload data.

```json
{
"success":   bool,
"sessionId": string,
"bytes":     number,       // present on success
"total":     number,       // file size in bytes
"error":     string        // present on failure
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_upload_progress() as events:` yields `StorageUploadProgressEvent`; `client.events('storageUploadProgress')` merges it with others.

<a id="event-storageUploadDone"></a>

### `storageUploadDone`

```lidl
event storageUploadDone(payload: tstr)
```

Emitted when uploadUrl() finishes.

```json
{
"success":   bool,
"sessionId": string,
"cid":       string,       // present on success
"error":     string        // present on failure
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_upload_done() as events:` yields `StorageUploadDoneEvent`; `client.events('storageUploadDone')` merges it with others.

<a id="event-storageDownloadProgress"></a>

### `storageDownloadProgress`

```lidl
event storageDownloadProgress(payload: tstr)
```

Emitted as downloadToUrl() or downloadChunks() receive data.

```json
{
"success":   true,
"sessionId": string,
"bytes":     number,       // file download (downloadToUrl)
"total":     number,       // file download; dataset size in bytes
"chunk":     string        // base64 chunk, stream download (downloadChunks)
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_download_progress() as events:` yields `StorageDownloadProgressEvent`; `client.events('storageDownloadProgress')` merges it with others.

<a id="event-storageDownloadDone"></a>

### `storageDownloadDone`

```lidl
event storageDownloadDone(payload: tstr)
```

Emitted when downloadToUrl() or downloadChunks() finishes.

```json
{
"success":   bool,
"sessionId": string,
"error":     string        // present on failure
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_download_done() as events:` yields `StorageDownloadDoneEvent`; `client.events('storageDownloadDone')` merges it with others.

<a id="event-storageDownloadManifestDone"></a>

### `storageDownloadManifestDone`

```lidl
event storageDownloadManifestDone(payload: tstr)
```

Emitted when downloadManifest() finishes.

```json
{
"success": bool,
"cid":     string,
"manifest": {              // present on success
"manifestVersion": number,
"treeCid":     string,
"datasetSize": number,
"blockSize":   number,
"filename":    string,
"mimetype":    string
},
"error":   string          // present on failure
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_download_manifest_done() as events:` yields `StorageDownloadManifestDoneEvent`; `client.events('storageDownloadManifestDone')` merges it with others.

<a id="event-storageRemoveDone"></a>

### `storageRemoveDone`

```lidl
event storageRemoveDone(payload: tstr)
```

Emitted when remove() finishes.

```json
{
"success": bool,
"cid":     string,
"error":   string          // present on failure
}
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `payload` | `tstr` | `payload: str` |

Python: `async with client.on_storage_remove_done() as events:` yields `StorageRemoveDoneEvent`; `client.events('storageRemoveDone')` merges it with others.
