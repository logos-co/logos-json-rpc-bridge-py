<!-- GENERATED by logos-bridge-codegen from the LIDL contract test_fullapi_cpp 1.0.0. Do not edit. -->
<!-- Source: lidl tests/fixtures/lidl/test_fullapi_cpp.lidl; generator: logos-bridge 0.1.0; reader: lidl 0.1.0 (2043d8b) -->

# `test_fullapi_cpp`

Universal C++ provider exercising every supported method parameter type, return type, and event parameter type. Reference implementation of the full_api contract; mirrored 1:1 by the Rust provider test_fullapi_rust so the same interface can be bound to either.

| | |
|---|---|
| Version | `1.0.0` |
| Category | `testing` |
| Depends on | nothing |
| Python clients | `AsyncTestFullapiCppClient`, `TestFullapiCppClient` |
| `interface_sha256` | `2f5db9ac25ff9bb96149f8a2f7f3f89d854c6d1f82d67c5a969b007dea6ff6c7` |
| `contract_sha256` | `6e92054db23a3b3bfb78179647a0d67315c48a2efd6e0a515b5f0b9bf6b7687c` |
| `shape_sha256` | `38b01e042e6d1eb2f0efcc3f5efd6909a16c0436263c8064269d36fdb8160f20` |

Calls go through `rpc.call`; `client` is an `AsyncTestFullapiCppClient` (await the calls)
or a `TestFullapiCppClient` (the same calls, blocking).

## Methods

| Method | Python | Returns |
|---|---|---|
| [`whoAmI`](#method-whoAmI) | `await client.who_am_i()` | `str` |
| [`echoString`](#method-echoString) | `await client.echo_string(v)` | `str` |
| [`echoBytes`](#method-echoBytes) | `await client.echo_bytes(v)` | `bytes` |
| [`echoInt`](#method-echoInt) | `await client.echo_int(v)` | `int` |
| [`echoUint`](#method-echoUint) | `await client.echo_uint(v)` | `int` |
| [`echoDouble`](#method-echoDouble) | `await client.echo_double(v)` | `float` |
| [`echoBool`](#method-echoBool) | `await client.echo_bool(v)` | `bool` |
| [`echoAny`](#method-echoAny) | `await client.echo_any(v)` | `Any` |
| [`echoStringList`](#method-echoStringList) | `await client.echo_string_list(v)` | `list[str]` |
| [`echoIntList`](#method-echoIntList) | `await client.echo_int_list(v)` | `list[int]` |
| [`echoUintList`](#method-echoUintList) | `await client.echo_uint_list(v)` | `list[int]` |
| [`echoDoubleList`](#method-echoDoubleList) | `await client.echo_double_list(v)` | `list[float]` |
| [`echoBoolList`](#method-echoBoolList) | `await client.echo_bool_list(v)` | `list[bool]` |
| [`echoList`](#method-echoList) | `await client.echo_list(v)` | `list[Any]` |
| [`echoMap`](#method-echoMap) | `await client.echo_map(v)` | `dict[str, Any]` |
| [`echoTriple`](#method-echoTriple) | `await client.echo_triple(i, s, b)` | `str` |
| [`doVoid`](#method-doVoid) | `await client.do_void()` | nothing |
| [`makeResult`](#method-makeResult) | `await client.make_result(ok)` | `LogosResult[Any]` |
| [`fireStringEvent`](#method-fireStringEvent) | `await client.fire_string_event(v)` | `bool` |
| [`fireBytesEvent`](#method-fireBytesEvent) | `await client.fire_bytes_event(v)` | `bool` |
| [`fireIntEvent`](#method-fireIntEvent) | `await client.fire_int_event(v)` | `bool` |
| [`fireUintEvent`](#method-fireUintEvent) | `await client.fire_uint_event(v)` | `bool` |
| [`fireDoubleEvent`](#method-fireDoubleEvent) | `await client.fire_double_event(v)` | `bool` |
| [`fireBoolEvent`](#method-fireBoolEvent) | `await client.fire_bool_event(v)` | `bool` |
| [`fireAnyEvent`](#method-fireAnyEvent) | `await client.fire_any_event(v)` | `bool` |
| [`fireStringListEvent`](#method-fireStringListEvent) | `await client.fire_string_list_event(v)` | `bool` |
| [`fireIntListEvent`](#method-fireIntListEvent) | `await client.fire_int_list_event(v)` | `bool` |
| [`fireUintListEvent`](#method-fireUintListEvent) | `await client.fire_uint_list_event(v)` | `bool` |
| [`fireDoubleListEvent`](#method-fireDoubleListEvent) | `await client.fire_double_list_event(v)` | `bool` |
| [`fireBoolListEvent`](#method-fireBoolListEvent) | `await client.fire_bool_list_event(v)` | `bool` |
| [`fireListEvent`](#method-fireListEvent) | `await client.fire_list_event(v)` | `bool` |
| [`fireMapEvent`](#method-fireMapEvent) | `await client.fire_map_event(v)` | `bool` |
| [`fireTripleEvent`](#method-fireTripleEvent) | `await client.fire_triple_event(i, s, b)` | `bool` |
| [`name`](#method-name) | `await client.name()` | `str` |
| [`version`](#method-version) | `await client.version()` | `str` |
| [`lidl`](#method-lidl) | `await client.lidl()` | `str` |

<a id="method-whoAmI"></a>

### `whoAmI`

```lidl
method whoAmI() -> tstr
```

**Returns** `tstr` (Python `str`).

Python: `await client.who_am_i()`

<a id="method-echoString"></a>

### `echoString`

```lidl
method echoString(v: tstr) -> tstr
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `tstr` | `v: str` |

**Returns** `tstr` (Python `str`).

Python: `await client.echo_string(v)`

<a id="method-echoBytes"></a>

### `echoBytes`

```lidl
method echoBytes(v: bstr) -> bstr
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `bstr` | `v: bytes` |

**Returns** `bstr` (Python `bytes`).

Python: `await client.echo_bytes(v)`

<a id="method-echoInt"></a>

### `echoInt`

```lidl
method echoInt(v: int) -> int
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `int` | `v: int` |

**Returns** `int` (Python `int`).

Python: `await client.echo_int(v)`

<a id="method-echoUint"></a>

### `echoUint`

```lidl
method echoUint(v: uint) -> uint
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `uint` | `v: int` |

**Returns** `uint` (Python `int`).

Python: `await client.echo_uint(v)`

<a id="method-echoDouble"></a>

### `echoDouble`

```lidl
method echoDouble(v: float64) -> float64
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `float64` | `v: float` |

**Returns** `float64` (Python `float`).

Python: `await client.echo_double(v)`

<a id="method-echoBool"></a>

### `echoBool`

```lidl
method echoBool(v: bool) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `bool` | `v: bool` |

**Returns** `bool` (Python `bool`).

Python: `await client.echo_bool(v)`

<a id="method-echoAny"></a>

### `echoAny`

```lidl
method echoAny(v: any) -> any
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `any` | `v: Any` |

**Returns** `any` (Python `Any`).
A legitimate value shaped like a provider refusal (`{code, message, origin}` with a known code) is reported as `ProviderRejection`.

Python: `await client.echo_any(v)`

<a id="method-echoStringList"></a>

### `echoStringList`

```lidl
method echoStringList(v: [tstr]) -> [tstr]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[tstr]` | `v: Sequence[str]` |

**Returns** `[tstr]` (Python `list[str]`).

Python: `await client.echo_string_list(v)`

<a id="method-echoIntList"></a>

### `echoIntList`

```lidl
method echoIntList(v: [int]) -> [int]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[int]` | `v: Sequence[int]` |

**Returns** `[int]` (Python `list[int]`).

Python: `await client.echo_int_list(v)`

<a id="method-echoUintList"></a>

### `echoUintList`

```lidl
method echoUintList(v: [uint]) -> [uint]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[uint]` | `v: Sequence[int]` |

**Returns** `[uint]` (Python `list[int]`).

Python: `await client.echo_uint_list(v)`

<a id="method-echoDoubleList"></a>

### `echoDoubleList`

```lidl
method echoDoubleList(v: [float64]) -> [float64]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[float64]` | `v: Sequence[float]` |

**Returns** `[float64]` (Python `list[float]`).

Python: `await client.echo_double_list(v)`

<a id="method-echoBoolList"></a>

### `echoBoolList`

```lidl
method echoBoolList(v: [bool]) -> [bool]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[bool]` | `v: Sequence[bool]` |

**Returns** `[bool]` (Python `list[bool]`).

Python: `await client.echo_bool_list(v)`

<a id="method-echoList"></a>

### `echoList`

```lidl
method echoList(v: [any]) -> [any]
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[any]` | `v: Sequence[Any]` |

**Returns** `[any]` (Python `list[Any]`).

Python: `await client.echo_list(v)`

<a id="method-echoMap"></a>

### `echoMap`

```lidl
method echoMap(v: {tstr: any}) -> {tstr: any}
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `{tstr: any}` | `v: Mapping[str, Any]` |

**Returns** `{tstr: any}` (Python `dict[str, Any]`).
A legitimate value shaped like a provider refusal (`{code, message, origin}` with a known code) is reported as `ProviderRejection`.

Python: `await client.echo_map(v)`

<a id="method-echoTriple"></a>

### `echoTriple`

```lidl
method echoTriple(i: int, s: tstr, b: bstr) -> tstr
```

| Parameter | LIDL | Python |
|---|---|---|
| `i` | `int` | `i: int` |
| `s` | `tstr` | `s: str` |
| `b` | `bstr` | `b: bytes` |

**Returns** `tstr` (Python `str`).

Python: `await client.echo_triple(i, s, b)`

<a id="method-doVoid"></a>

### `doVoid`

```lidl
method doVoid()
```

**Returns** nothing (the provider answers `true`; Python gets `None`).

Python: `await client.do_void()`

<a id="method-makeResult"></a>

### `makeResult`

```lidl
method makeResult(ok: bool) -> result
```

| Parameter | LIDL | Python |
|---|---|---|
| `ok` | `bool` | `ok: bool` |

**Returns** `result` (Python `LogosResult[Any]`).
`success: false` is an answer, not an exception; `LogosResult.unwrap()` raises it.

Python: `await client.make_result(ok)`

<a id="method-fireStringEvent"></a>

### `fireStringEvent`

```lidl
method fireStringEvent(v: tstr) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `tstr` | `v: str` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_string_event(v)`

<a id="method-fireBytesEvent"></a>

### `fireBytesEvent`

```lidl
method fireBytesEvent(v: bstr) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `bstr` | `v: bytes` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_bytes_event(v)`

<a id="method-fireIntEvent"></a>

### `fireIntEvent`

```lidl
method fireIntEvent(v: int) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `int` | `v: int` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_int_event(v)`

<a id="method-fireUintEvent"></a>

### `fireUintEvent`

```lidl
method fireUintEvent(v: uint) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `uint` | `v: int` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_uint_event(v)`

<a id="method-fireDoubleEvent"></a>

### `fireDoubleEvent`

```lidl
method fireDoubleEvent(v: float64) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `float64` | `v: float` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_double_event(v)`

<a id="method-fireBoolEvent"></a>

### `fireBoolEvent`

```lidl
method fireBoolEvent(v: bool) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `bool` | `v: bool` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_bool_event(v)`

<a id="method-fireAnyEvent"></a>

### `fireAnyEvent`

```lidl
method fireAnyEvent(v: any) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `any` | `v: Any` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_any_event(v)`

<a id="method-fireStringListEvent"></a>

### `fireStringListEvent`

```lidl
method fireStringListEvent(v: [tstr]) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[tstr]` | `v: Sequence[str]` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_string_list_event(v)`

<a id="method-fireIntListEvent"></a>

### `fireIntListEvent`

```lidl
method fireIntListEvent(v: [int]) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[int]` | `v: Sequence[int]` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_int_list_event(v)`

<a id="method-fireUintListEvent"></a>

### `fireUintListEvent`

```lidl
method fireUintListEvent(v: [uint]) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[uint]` | `v: Sequence[int]` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_uint_list_event(v)`

<a id="method-fireDoubleListEvent"></a>

### `fireDoubleListEvent`

```lidl
method fireDoubleListEvent(v: [float64]) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[float64]` | `v: Sequence[float]` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_double_list_event(v)`

<a id="method-fireBoolListEvent"></a>

### `fireBoolListEvent`

```lidl
method fireBoolListEvent(v: [bool]) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[bool]` | `v: Sequence[bool]` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_bool_list_event(v)`

<a id="method-fireListEvent"></a>

### `fireListEvent`

```lidl
method fireListEvent(v: [any]) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `[any]` | `v: Sequence[Any]` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_list_event(v)`

<a id="method-fireMapEvent"></a>

### `fireMapEvent`

```lidl
method fireMapEvent(v: {tstr: any}) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `v` | `{tstr: any}` | `v: Mapping[str, Any]` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_map_event(v)`

<a id="method-fireTripleEvent"></a>

### `fireTripleEvent`

```lidl
method fireTripleEvent(i: int, s: tstr, b: bstr) -> bool
```

| Parameter | LIDL | Python |
|---|---|---|
| `i` | `int` | `i: int` |
| `s` | `tstr` | `s: str` |
| `b` | `bstr` | `b: bytes` |

**Returns** `bool` (Python `bool`).

Python: `await client.fire_triple_event(i, s, b)`

<a id="method-name"></a>

### `name`

```lidl
method name() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's name, as declared in its metadata.

**Returns** `tstr` (Python `str`).

Python: `await client.name()`

<a id="method-version"></a>

### `version`

```lidl
method version() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's version, as declared in its metadata.

**Returns** `tstr` (Python `str`).

Python: `await client.version()`

<a id="method-lidl"></a>

### `lidl`

```lidl
method lidl() -> tstr
```

A derived built-in: every module answers it, and no author writes it.

The module's canonical LIDL interface document.

**Returns** `tstr` (Python `str`).

Python: `await client.lidl()`

## Events

<a id="event-stringEvent"></a>

### `stringEvent`

```lidl
event stringEvent(v: tstr)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `tstr` | `v: str` |

Python: `async with client.on_string_event() as events:` yields `StringEventEvent`; `client.events('stringEvent')` merges it with others.

<a id="event-bytesEvent"></a>

### `bytesEvent`

```lidl
event bytesEvent(v: bstr)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `bstr` | `v: bytes` |

Python: `async with client.on_bytes_event() as events:` yields `BytesEventEvent`; `client.events('bytesEvent')` merges it with others.

<a id="event-intEvent"></a>

### `intEvent`

```lidl
event intEvent(v: int)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `int` | `v: int` |

Python: `async with client.on_int_event() as events:` yields `IntEventEvent`; `client.events('intEvent')` merges it with others.

<a id="event-uintEvent"></a>

### `uintEvent`

```lidl
event uintEvent(v: uint)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `uint` | `v: int` |

Python: `async with client.on_uint_event() as events:` yields `UintEventEvent`; `client.events('uintEvent')` merges it with others.

<a id="event-doubleEvent"></a>

### `doubleEvent`

```lidl
event doubleEvent(v: float64)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `float64` | `v: float` |

Python: `async with client.on_double_event() as events:` yields `DoubleEventEvent`; `client.events('doubleEvent')` merges it with others.

<a id="event-boolEvent"></a>

### `boolEvent`

```lidl
event boolEvent(v: bool)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `bool` | `v: bool` |

Python: `async with client.on_bool_event() as events:` yields `BoolEventEvent`; `client.events('boolEvent')` merges it with others.

<a id="event-anyEvent"></a>

### `anyEvent`

```lidl
event anyEvent(v: any)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `any` | `v: Any` |

Python: `async with client.on_any_event() as events:` yields `AnyEventEvent`; `client.events('anyEvent')` merges it with others.

<a id="event-stringListEvent"></a>

### `stringListEvent`

```lidl
event stringListEvent(v: [tstr])
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `[tstr]` | `v: list[str]` |

Python: `async with client.on_string_list_event() as events:` yields `StringListEventEvent`; `client.events('stringListEvent')` merges it with others.

<a id="event-intListEvent"></a>

### `intListEvent`

```lidl
event intListEvent(v: [int])
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `[int]` | `v: list[int]` |

Python: `async with client.on_int_list_event() as events:` yields `IntListEventEvent`; `client.events('intListEvent')` merges it with others.

<a id="event-uintListEvent"></a>

### `uintListEvent`

```lidl
event uintListEvent(v: [uint])
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `[uint]` | `v: list[int]` |

Python: `async with client.on_uint_list_event() as events:` yields `UintListEventEvent`; `client.events('uintListEvent')` merges it with others.

<a id="event-doubleListEvent"></a>

### `doubleListEvent`

```lidl
event doubleListEvent(v: [float64])
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `[float64]` | `v: list[float]` |

Python: `async with client.on_double_list_event() as events:` yields `DoubleListEventEvent`; `client.events('doubleListEvent')` merges it with others.

<a id="event-boolListEvent"></a>

### `boolListEvent`

```lidl
event boolListEvent(v: [bool])
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `[bool]` | `v: list[bool]` |

Python: `async with client.on_bool_list_event() as events:` yields `BoolListEventEvent`; `client.events('boolListEvent')` merges it with others.

<a id="event-listEvent"></a>

### `listEvent`

```lidl
event listEvent(v: [any])
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `[any]` | `v: list[Any]` |

Python: `async with client.on_list_event() as events:` yields `ListEventEvent`; `client.events('listEvent')` merges it with others.

<a id="event-mapEvent"></a>

### `mapEvent`

```lidl
event mapEvent(v: {tstr: any})
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `v` | `{tstr: any}` | `v: dict[str, Any]` |

Python: `async with client.on_map_event() as events:` yields `MapEventEvent`; `client.events('mapEvent')` merges it with others.

<a id="event-tripleEvent"></a>

### `tripleEvent`

```lidl
event tripleEvent(i: int, s: tstr, b: bstr)
```

| Parameter | LIDL | Python attribute |
|---|---|---|
| `i` | `int` | `i: int` |
| `s` | `tstr` | `s: str` |
| `b` | `bstr` | `b: bytes` |

Python: `async with client.on_triple_event() as events:` yields `TripleEventEvent`; `client.events('tripleEvent')` merges it with others.
